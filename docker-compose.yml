version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mdcgs-frontend
    restart: unless-stopped
    ports:
      - "7785:80"
    networks:
      default:
        external: true
        name: mdcgs-backend_default
